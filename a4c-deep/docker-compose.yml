version: '3'

services:
  a4c:
    container_name: a4c
    image: indigodatacloud/alien4cloud-deep:2.0-r0
    restart: unless-stopped
    env_file:
      - .env
    ports:
            - '8082:8080'
    volumes:
      - ~/Documents/a4c-deep:/data
      - /etc/ssl:/certs:ro


